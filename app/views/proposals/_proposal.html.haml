%tr.proposal
  %td= proposal_counter + 1
  %td.title= proposal.title
  %td
    = proposal.speaker_name
    - if proposal.website?
      %br
      = link_to(proposal.website,proposal.website)
  %td= proposal.description
  %td= proposal.bio
